name : schdule 

on :
    schedule:
        - cron: "0 0  * * *"

jobs:
     run-script :
        runs-on: ubuntu-latest          

steps:
     - name : message printing
       run : echo " schedule workflow "
        
